import { Container, Content } from "./shareStyles"

export function ShareContainer({ children }) {
  return <Container>{children}</Container>
}

export function ShareContent({ children }) {
  return <Content>{children}</Content>
}
